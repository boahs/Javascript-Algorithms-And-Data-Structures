function testTruth(truth) {
	if (truth == 420) { 
	return "Right on brother;"
	}
	return "Oh well. Maybe next time.";
}
testTruth(16);
testTruth(420);
testTruth(41); // == can compare TWO values and returns true if they're equvalanet. Such as a string, and number. 