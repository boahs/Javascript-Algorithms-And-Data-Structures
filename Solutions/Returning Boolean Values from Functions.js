function isLess(a,b) {
    return a < b;
}

isLess(10, 15); 

// function isLess(a, b){
//    if (a === b) {
//       return true;
//   } else {
//       return false;
//   }
// } 

// We can eliminate the unnecessary lines of code, and just return the boolean value from the function using the less than operator. 
